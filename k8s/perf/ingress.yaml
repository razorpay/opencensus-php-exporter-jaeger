apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: auth
  namespace: perf
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: perf-auth.razorpay.in
    http:
      paths:
      - path: /
        backend:
          serviceName: auth
          servicePort: 80
