apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: auth
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    name: auth
  sessionAffinity: None
  type: ClusterIP
