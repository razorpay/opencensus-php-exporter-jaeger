apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: auth
spec:
  sessionAffinity: None
  type: ClusterIP
  selector:
    name: auth  
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
