apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  generation: 1
  name: auth
  namespace: auth
spec:
  rules:
  - host: stage-auth.razorpay.com
    http:
      paths:
      - backend:
          serviceName: auth
          servicePort: 80
        path: /
